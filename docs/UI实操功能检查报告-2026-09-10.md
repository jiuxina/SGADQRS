# 赛友 TeamUp（SCMS）UI 实操功能完整性与操作容错性检查报告

- **日期**：2026-09-10（下午，接续同日早间《全功能检查报告》之后的**纯浏览器实操**复检）
- **方法**：ZCode 内置浏览器（Chromium 1280×720）真实操作——点击、键盘输入、下拉选择、文件下载事件捕获、截图视觉检查。**全部功能逐项以 UI 操作验证，未使用任何 API 测试脚本**；仅两类场景使用 HTTP/SQL 交叉验证并在文中明确标注：① 导出文件字节内容（浏览器已捕获下载事件，字节为旁证）；② 破坏性守卫（删自己账号）与数据清理核对。
- **环境**：MySQL 8（Docker `mysql-scms`）＋ Spring Boot 3.2.5（:8080）＋ Vite/React 19（:3000）。检查中全栈曾中断一次，按 README 通道重启后继续；运行中后端编译产物晚于全部源码（`mvn spring-boot:run` 现场编译），与 HEAD 提交（D1–D22 修复后）一致。
- **账号**：admin / T2024001（张教授）/ S20210001（王明）/ stu_ui01（本次新建临时账号，检查结束已删除）。种子账号密码未改动。

---

## 一、结论摘要

**总体判断：主流程功能完整、状态机与守卫在 UI 侧表现扎实；D1–D22 修复在 UI 侧复测全部成立。新发现 2 项中危前端缺陷（F1 顶栏搜索断链、F2 统计趋势图渲染失真）、1 项中低危假字段（F3 邮箱/手机）、若干低危口径与体验问题（F4–F8）。**

| 严重度 | 编号 | 问题 |
|---|---|---|
| 中 | **F1** | 学生竞赛页不消费顶栏搜索的 `?search=` 参数 → 顶栏搜索对学生实际无效（教师/管理员页正常） |
| 中 | **F2** | 统计页「组队趋势」图渲染失真：API 数据正确（6月12/8月36/9月1），图上所有点贴 0 基线，Y 轴却正确撑到 36 |
| 中低 | **F3** | 个人中心「邮箱/手机号」为假字段：后端无列无 DTO，提交恒显示「更新成功」但值不持久（刷新即丢） |
| 低 | **F4** | 竞赛状态徽章与筛选/统计口径不一致：「数据挖掘挑战赛」卡片显示「进行中」（库存 status=3 不向下修正），但「进行中」筛选与统计「进行中=0」均按时间派生 |
| 低 | **F5** | 统计页「获奖数 42」「获奖分布」含 13 条未发布成绩（已发布仅 29），UI 无口径标注 |
| 低 | **F6** | 演示数据生成器公告不幂等：同一公告重复插入 14 行（学生公告页同题渲染 8 次），本次已清理回基线 6 条 |
| 低 | **F7** | 每个页面首次进入自动弹「使用教程」遮罩并拦截点击（实测两次导致操作失败需先关闭），跨账号切换均重新弹出 |
| 低 | **F8** | 已通过/待审核队伍仍显示「指导老师」按钮，点击保存后才提示被拒——守卫末端兜底，UI 应前置隐藏（同型：待审核队「解散队伍」为有意设计不算此列） |

---

## 二、功能完整性矩阵（按角色 × 功能，全部浏览器实操）

### 2.1 匿名与登录面（10 项）

| 功能 | 操作 | 结果 |
|---|---|---|
| 登录页渲染 | 打开 :3000 | ✅ 身份三选、账号密码、密码显隐、记住账号、忘记密码、README 提示齐全 |
| 空表单提交 | 直接点登录 | ✅ 停留原页，行内提示「请输入账号和密码」 |
| 错误密码 | 王明 + 错密码 | ✅ 「账号或密码错误」 |
| 不存在账号 | nosuchuser999 | ✅ 同文案（防枚举） |
| 角色不符 | 选「管理员」登王明 | ✅ 同文案，不泄露角色差异 |
| SQLi 用户名 | `admin' OR '1'='1` | ✅ 同文案，无害 |
| 密码显隐切换 | 点小眼睛 | ✅ type password↔text |
| 忘记密码 | 点击 | ✅ 提示「请联系管理员重置密码」 |
| 记住账号 | 勾选→登录→登出 | ✅ 账号回填（localStorage `scms_remember_user`，保存时机=登录成功；验证后已清除恢复） |
| 三角色登录 | admin/T2024001/S20210001 | ✅ 各自落到角色首页 |

### 2.2 学生端（S20210001 王明）

| 功能 | 结果与实测细节 |
|---|---|
| 概览 | ✅ 问候语、可报名12/已参赛N 实时联动（建队后 3→4）、竞赛卡、报名中列表、时间节点、快捷入口；教程向导 3 步闭环 |
| 顶栏搜索 | ❌ **F1**：跳 `/student/competitions?search=英语` 后列表仍「共 14 个」、页内搜索框空——参数被忽略（源码定位：AdminCompetitions.tsx:46-52 有 search 同步 effect，StudentCompetitions.tsx 无） |
| 竞赛列表 | ✅ 页内搜索（英语→2 条、清空→14、无结果词→「共 0 个/暂无符合条件的竞赛」空态）、状态筛选（报名中/进行中/已结束/全部）正确 |
| 竞赛详情 | ✅ 简介/规则/奖项/组队招募/倒计时/参赛情况全区块；报名后翻转为「报名状态：你已在参赛队伍中」 |
| 个人赛报名 | ✅ 确认弹窗（明示自动建 1 人队并提交审核）→「报名成功！队伍已自动提交审核」，队数 6→7；解散后 7→6、按钮恢复（位释放） |
| 组队中心 | ✅ 四子 tab（我的队伍/收到的申请/收到的邀请/我发出的）；分页与每页条数控件；卡片按钮矩阵随状态变化正确（组建中=提交/导师/解散；待审核=导师/解散；已通过=仅导师；已拒绝=重新提交/导师/解散） |
| 创建团队 | ✅ 表单校验（空提交「请选择竞赛」）；一人一赛一队拦截（「你已参加了该竞赛的队伍，不可重复报名」）；个人赛选项标注「创建即报名」 |
| 换导师 | ✅ 组建中队成功（「指导老师已更新」）；已通过队被拒（「提交审核后不可更换指导老师（被驳回后可调整）」）＝D16 UI 复测；下拉仅 6 名教师＝D4 收窄生效；待审核队成员行无移除入口＝D2 冻结前置到 UI |
| 提交/解散 | ✅ 提交审核 toast + 状态迁移；解散有二次确认（明示连带下架招募帖）；已拒绝队可解散 |
| 招募广场 | ✅ 类型（求组8/招募9/全部17）、竞赛、关键字三筛选（AND 组合正确）；发帖表单：竞赛选择后动态出现「关联队伍」（仅本人该竞赛队）、发布按钮初始禁用、联系方式选填；编辑保存（竞赛锁定）；超长标题结构化拒绝「标题不能超过 100 字」＝D11；关闭帖即时下架 18→17 |
| 帖子详情 | ✅ 队长视角（关闭/编辑，无申请入口）与访客视角（申请加入）正确；装配队长资料卡+实时人数「1/4 人」 |
| 消息中心 | ✅ 通知/公告/只看未读/全部已读；公告草稿不下发（学生端 0 草稿）＝D7 |
| 参赛历史 | ✅ 时间线 5 条（含新建队实时记录）、统计卡、成绩单仅显已发布 2 条；导出成绩单触发下载（旁证：API 返回合法 xlsx 4065B） |
| 个人中心 | ✅ 真名锁定；昵称 50 字边界通过、超限「昵称最多 50 个字符」；XSS 串（`<script>`/`<img onerror>`）按原文存取、无脚本执行（React 转义）；改密四类负例（空/原密码错/新密码<6/两次不一致）全拦截且未改密；❌ **F3**：邮箱/手机输入后提示「更新成功」但后端无字段（sys_user 无 email/phone 列，DTO/Service 零处理），刷新即丢 |
| 公开主页 | ✅ 昵称/院系班级/参赛2次/获奖2次/获奖率100%/已发布获奖记录；本人视角带「编辑我的资料」 |
| 越权与兜底 | ✅ /admin/users、/teacher/dashboard → 弹回 /student/dashboard；未知路径 → 角色首页（D22 生效） |
| 登出 | ✅ 回登录页，会话失效 |

### 2.3 教师端（T2024001 张教授）

| 功能 | 结果 |
|---|---|
| 概览 | ✅ 我发布 9 项、最近竞赛报名数实时（新建队后同步）、快捷入口 |
| 竞赛管理 | ✅ 表格列表、状态 tab（草稿/已发布/进行中/已结束）、页内搜索；**顶栏搜索 ?search= 生效**（回填+过滤 1 条）——对照坐实 F1 仅学生页缺失 |
| 发布竞赛 | ✅ 空提交 6 字段逐行红字；时间倒序「报名截止时间必须晚于报名开始时间」；人数 999 →「每队人数须在 1 ~ 99 之间」；保存草稿→草稿 tab 可见（仅本人草稿＝D6）；编辑页无 status 注入面＝D8；「保存并发布」迁移状态 |
| 竞赛详情 | ✅ 基本信息/参赛队伍/成绩管理三 tab + 编辑入口；⚠ 详情页成绩 tab 对数学建模显示「暂无成绩记录」而成绩录入页同竞赛有 3 条——两处查询口径不同（详情按队伍维度、录入按学生维度），建议 UI 标注（低危观察项） |
| 成绩录入 | ✅ 竞赛下拉仅本人发布竞赛（含新建 126）＝D1 写入口收窄；批量录入预置全部参队学生（防孤儿）；空分行自动过滤（确认层显示「1 条」）；重复学生被拒「该竞赛下已存在相同学生/队伍的成绩记录，请直接编辑原记录」；二次确认层；单条编辑弹窗（奖项下拉=该竞赛自定义奖项 5 档）取消无副作用；发布成绩有确认层且幂等安全（不刷新 publish_time、不重复通知——DB 核对） |
| 团队管理 | ✅ 竞赛团队（我发布竞赛的队，新建队实时入列、状态「已提交」）/指导团队（7 队）双 tab、队名搜索空态、导出触发下载 |
| 越权与铃铛 | ✅ /admin/users、/student/teams、/admin/competitions/1 全部弹回 /teacher/dashboard |
| 登出 | ✅ |

### 2.4 管理员端（admin）

| 功能 | 结果 |
|---|---|
| 系统总览 | ✅ 竞赛分布（15 项=13 已发布+2 已结束，新建赛实时）、待审核队列 11 队（新队入列）、用户/队伍计数、快捷入口 |
| 竞赛中心 | ✅ 列表（发布者列、导出、搜索、状态筛选）；/admin/teams、/admin/grades 重定向到对应 tab ✅；竞赛详情三 tab 完整（海报图、规则、队伍审核、成绩管理）；队伍详情（成员卡、5/5 满员、队长标） |
| 队伍审核 | ✅ 状态筛选五档、竞赛下拉、搜索；拒绝流程：原因弹窗（明示「将通知全体成员」）→「已拒绝，已通知全体成员」→ 已拒绝 tab 可见 → 学生端出现「重新提交审核」；通过/重提闭环由同日 API 回归锁死 |
| 用户管理 | ✅ 统计卡（58 人实时）、搜索（姓名/用户名/院系）、角色筛选（教师 6 人口径正确）、清空恢复；新增用户（角色三选、创建成功入列、默认密码可登录）；编辑（改名保存即时反映、超长姓名「姓名不能超过 50 字」＝D11、重置密码二次确认→「密码重置成功」→新密码生效）；禁用→行态+按钮翻「启用」+登录被拒「账号已被禁用，请联系管理员」；批量禁用/启用（勾选行→批量条出现→确认→toast→状态回切）；表头全选/反选；导出用户触发下载＝D3 修复 UI 复测 |
| 删除守卫 | ✅ 队长删除拒绝「该账号仍在担任队长，请先解散或转让队伍」（解散后删除成功）＝D21；删自己：UI 未实点（防自毁），API 级验证「不能删除当前登录账号」+admin 无恙（旁证） |
| 数据统计 | ⚠ **F2 趋势图渲染失真**（截图证据）；**F5 获奖数含未发布**；✅ 其余 KPI 正确（进行中=0 时间派生＝D18）、热度排行、获奖分布条、CSV 导出触发下载；无日期范围控件残留＝D10 |
| 公告管理 | ✅ 全链实测：空提交拦截、存草稿（「草稿已保存」+草稿筛选可见+学生端不下发）、编辑发布（「更新成功」）、置顶（「已置顶」+首位+徽章）、取消置顶、关键字搜索命中 1 条＝D10 修复 UI 复测、撤回（确认明示变草稿）、删除（确认+消失）；⚠ 操作前存在 14 行重复公告噪音＝F6（已清理） |
| 越权 | ✅ /student/teams、/teacher/competitions 弹回 /admin/dashboard |

### 2.5 跨账号闭环（临时账号 stu_ui01，全 UI 操作）

| 步骤 | 结果 |
|---|---|
| admin 建 stu_ui01 → 禁用 → 登录拒绝 → 批量启用 → 重置密码 → 123456 登录成功 | ✅ 全链 |
| stu_ui01 建队（绿色低碳 3 人队）+ 发招募帖 | ✅ 关联队伍动态字段二次验证 |
| 王明申请加入（留言保存、帖显示 1/3 实时） | ✅ 队长收件箱装配申请人完整资料卡+备注+关联帖 |
| 王明重复申请 | ✅ 「已有待处理的申请，请等待队长处理，勿重复提交」＝D14 UI 复测 |
| 队长同意 → 王明入队（成员 2 人） | ✅ 双端状态同步 |
| 队长提交审核 → 成员行无移除入口 | ✅ D2 冻结在 UI 前置（后端闸门由 API 回归锁死） |
| admin 驳回 430 → stu_ui01 解散 → **招募帖连带下架** | ✅ 解散确认文案兑现（广场不再显示该帖） |
| admin 删 stu_ui01：队长态被拒 → 解散后成功 | ✅ D21 双守卫 |
| 王明解散已拒绝的 427 | ✅ 闭环 |
| 张教授删除测试竞赛 126（二次确认「确定要删除此竞赛吗？」） | ✅ 列表即消失 |

---

## 三、操作容错性清单

**被正确拦截/处理的异常操作（本次 UI 实测 40+ 项）**：错密码/不存在账号/角色不符/SQLi 登录、空表单×4（登录/建队/发帖/发布竞赛/新增用户/公告）、时间倒序、人数 0/999 越界、昵称/姓名/帖标题超长、重复报名（一人一赛一队）、重复申请（D14）、重复成绩录入判重、待审核队换导师（D16）、已通过队换导师、解散确认、删除确认×3（用户/竞赛/公告）、撤回确认、批量操作确认、队长删账号（D21）、自删账号（API 旁证）、禁用账号登录、匿名/跨角色 URL 全部重定向、未知路径角色首页兜底、导出按钮失败路径未触发（全部成功）、XSS 串存取不执行、下载事件全部正常捕获。

**未拦截/体验劣化**：F1（搜索参数静默丢弃）、F3（假字段假成功）、F2（图渲染失真但数据正确）、F4/F5（口径不一致）、F7（教程遮罩拦截点击）、F8（按钮后置守卫）。

---

## 四、D1–D22 修复 UI 侧复测对照

| # | UI 复测结论 |
|---|---|
| D1 | ✅ 教师成绩下拉仅本人竞赛（写入口收窄）；跨教师改分由 API 回归锁死 |
| D2 | ✅ 待审核队成员行无移除入口；「提交审核后不可更换指导老师」文案；积压申请闸门 API 锁死 |
| D3 | ✅ 导出用户下载触发 |
| D4 | ✅ 学生端教师下拉 6 人简表 |
| D5 | ✅ 教师团队列表无他人数据行 |
| D6 | ✅ 教师草稿 tab 仅本人 |
| D7 | ✅ 公告草稿学生端 0 条 |
| D8 | ✅ 编辑表单无 status 注入面 |
| D9 | ✅ 登录页无注册入口（API-only） |
| D10 | ✅ 公告搜索生效；统计页无日期控件；⚠ 但发现同类新缺陷 F1（学生竞赛页 search 未同步，修复只覆盖了 admin/teacher） |
| D11 | ✅ 帖标题/昵称/姓名三处超长均结构化拒绝（无 HTTP 500） |
| D12 | ⚪ 并发双击 UI 不可复现，API 锁死 |
| D13 | ⚪ 批量禁用 UI 无 status 注入面 |
| D14 | ✅ 重复申请拒绝 |
| D15 | ✅ 关闭帖无编辑入口（UI 不可达） |
| D16 | ✅ 冻结后换导师拒绝 |
| D17 | ⚪ 维持防探测设计 |
| D18 | ✅ 进行中=0 时间派生正确；⚠ 未完全收敛：F4 徽章口径、F5 获奖含未发布 |
| D19 | ⚪ 通知降级 UI 不可观测，API 锁死 |
| D20 | ✅ 导出均正常触发无半截文件 |
| D21 | ✅ 队长删除守卫 + 删竞赛级联 + 解散连带下架帖；自删守卫 API 旁证 |
| D22 | ✅ 未知路径角色首页兜底；死代码无 UI 残留 |

---

## 五、数据完整性与清理记录

- 检查过程写入的全部测试数据均已清除：临时队 426–430（解散即物理清除，成员行归零）、测试招募帖 2 条、测试申请 1 条、测试通知 6 条、测试竞赛 126、临时账号 stu_ui01。
- **发现并清理 F6**：公告表存在生成器重复插入噪音 14 行（「2026年秋季学期…」×8、「平台使用小贴士」×8，id 间隔 32 与 gen_demo_data 批量一致）。按「同题保留最新一条」清理后公告=6（init 4 + demo 2），学生公告页终验无重复。
- 清理后 8 表行数：sys_user 57、competition 14、competition_team 48、competition_team_member 109、competition_result 42、recruit_post 18、community_request 20、sys_notification 42——与规范基线一致；三角种账号登录复验正常，密码未动。
- 建议：`gen_demo_data.py` 对 user_id=0 公告的清理条件补全（当前重复执行会累积），可参照其对 recruit_post/community_request 的硬删幂等方式。

---

## 六、覆盖声明与局限（如实）

- **已覆盖**：三角色全部 26 页面可达路由 + 重定向/守卫/兜底；登录/注册面（无注册 UI）；报名-建队-提交-审核-驳回-重提入口-解散全状态机；招募发帖-编辑-关闭-申请-审批-入队-连带下架；成绩批量/单条/发布/幂等；用户 CRUD/批量/禁用/重置/删除守卫；公告全链；统计与四路导出；消息中心与铃铛；跨账号闭环（临时账号全链后清理）。
- **未覆盖/降级验证（诚实声明）**：① 删自己账号仅 API 旁证（UI 实点有自毁会话风险）；② 并发双击、通知发送失败降级、教师跨归属改分负例、导出内存化——UI 无法构造，依赖同日 API 回归锁（263 断言）；③ 头像上传/裁剪（IAB 不支持文件选择器，未测）；④ 移动端窄屏未遍历；⑤ 教程「知道了」的跨会话持久性未单独验证（切账号会重弹，属按账号状态）；⑥ 浏览器为 IAB Chromium 1280×720 单一视口。
- **工具局限说明**：IAB 后端对部分带持续动画的弹窗元素存在 Playwright actionability 超时（登录页、新增用户/编辑弹窗等），改用坐标点击/DOM 事件派发完成，均为真实浏览器事件路径，不构成产品缺陷证据；`fill("")` 对该后端不可靠，清空一律用逐字符退格（已据此纠正一次「搜索清空失效」的误报）。

---

## 七、修复优先级建议

1. **P1**：F1（StudentCompetitions 补 `?search=` 同步 effect，与 Admin/Teacher 同构，5 行改动）；F3（补 sys_user email/phone 列+DTO 与格式校验，或移除两字段）；F2（趋势图坐标映射排查，疑似 y 缩放/取整错误）。
2. **P2**：F4（徽章统一走时间派生，或派生时向下修正库存 3→2）；F5（获奖统计加 is_published=1 或 UI 标注口径）；F6（gen_demo_data 公告幂等清理）。
3. **P3**：F7（教程自动弹出降频/首次一次全局记忆）；F8（按状态隐藏「指导老师」等后置守卫按钮）。

## 八、修复结果（2026-09-10 同日完成，回归锁死）

F1–F8 全部处置完毕，逐项复验证据如下：

| # | 处置 | 实现与复验 |
|---|---|---|
| F1 | ✅ 已修 | `StudentCompetitions.tsx` 补 `useSearchParams` 同步 effect（与 Admin 同构）。浏览器复验：`?search=英语` → 页内框回填「英语」+「共 2 个」+ 不含无关卡 |
| F2 | ✅ 已修 | `AdminStats.tsx` 月份列补 `height:100% + justifyContent:flex-end`（原容器无确定高度致百分比塌缩）。DOM 量测复验：柱高 [0,0,51,0,152,0]px、点位移 [173,173,223,173,325,173]，与 12/36/1 数据成比例 |
| F3 | ✅ 已修 | `ProfilePage.tsx` 移除邮箱/手机两字段（state/水合/提交/JSX 四处）。浏览器复验：字段消失、简介等其余字段正常。**未动数据库结构**（项目规则：schema 变更需另行确认；两字段本无任何后端支撑） |
| F4 | ✅ 已修 | `CompetitionService.autoUpdateStatus` 改全时间双向派生。API 复验：库存 3 未开赛→徽章 2；库存 2 已过赛期→徽章 4；「进行中」筛选=1 条与徽章一致。boundary 新增 2 断言锁死 |
| F5 | ✅ 已修 | `StatsService.awardDistribution` 补 `isPublished=1`。API 复验 sum=29=DB 已发布获奖行数；统计页 KPI「获奖数」UI 显示 29。boundary 新增 1 断言锁死 |
| F6 | ✅ 已修 | `gen_demo_data.py` 清理段按标题删本脚本 2 条公告（init 4 条种子不受影响）。实跑复验：重跑后公告恒 6 条、19 项自校验全绿 |
| F7 | ✅ 已修 | `DesktopLayout.tsx` 自动弹教程收敛为仅角色首页一次。浏览器复验：竞赛/组队/详情页均不再出现遮罩；顶栏「使用教程」按钮仍可用 |
| F8 | ✅ 已修 | `StudentTeams.tsx`/`TeamDetail.tsx` 指导老师按钮按 `status∈{0,3}` 前置隐藏。浏览器复验按钮矩阵：组建中=提交+导师+解散；待审核=仅解散；已通过=无（原三态均显导师按钮） |

**回归汇总**：smoke 65/65、member_flow 31/31、boundary **268/268**（+5 新断言：F4×3 建立/向下/向上、F5×1、含建队清理）、`npm run build` 通过（仅既有 chunk>500KB 警告）、Playwright e2e 1/1、`tsc -b` 零错误；后端已带新代码重启。检查全程写入的临时队（431/432）已解散，8 表行数与规范基线逐项一致（users 57 / comps 14 / teams 48 / members 109 / results 42 / posts 18 / reqs 20 / 公告 6）。

**遗留说明**：F3 若未来确需收集联系方式，应走「schema 变更确认流程」补列+校验，而非恢复假字段；F7 的教程「每标题一次」持久化机制本身原本就存在，本次仅收敛自动弹出的范围。

---

*本报告全部结论来自浏览器实操证据；截图证据：统计页 F2 现场图（会话工件）。*
